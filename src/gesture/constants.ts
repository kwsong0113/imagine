import { Point } from './types';

export const NumPointClouds = 0;
export const NumPoints = 32;
export const Origin = new Point(0, 0);
export const MaxIntCoord = 1024;
export const LUTSize = 64;
export const LUTScaleFactor = MaxIntCoord / LUTSize;
